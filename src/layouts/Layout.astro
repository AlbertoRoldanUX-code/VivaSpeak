---
import CookieBanner from "../components/CookieBanner.astro";
import SchemaMarkup from "../components/SchemaMarkup.astro";
import "../../styles/global.css";

const { 
  lang = "es",  
  title = "VivaSpeak", 
  description = "",
  canonical, 
  ogImage = "https://vivaspeak.com/assets/og-image.png", 
  ogType = "website", 
  keywords = "agente de voz IA, asistente virtual, automatización llamadas, WhatsApp Business API, chatbot IA, gestión clínicas, inmobiliarias, reservas automáticas, atención cliente 24/7, integración con agendas",
  alternateEs = "",
  alternateEn = "",
  schemaFaq = [],
  robots = "index, follow",
  breadcrumb = [],
  schemaArticle = null
} = Astro.props;
const currentUrl = canonical || Astro.url.href;
const fullOgImage = ogImage.startsWith("http") ? ogImage : `https://vivaspeak.com${ogImage.startsWith("/") ? "" : "/"}${ogImage}`;

---
<!DOCTYPE html>
<html lang={lang}>
  <head>
  
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#16a085" />
    <title>{title}</title>
    
    <!-- ✅ Robots -->
    <meta name="robots" content={robots} />
    
    <!-- ✅ Canonical -->
    {canonical && <link rel="canonical" href={canonical} />}
    
    <!-- ✅ Meta description -->
    <meta name="description" content={description} />
    
    <!-- ✅ Keywords -->
    <meta name="keywords" content={keywords} />
    
    <!-- ✅ Author -->
    <meta name="author" content="VivaSpeak" />
    
    <!-- ✅ Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullOgImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:url" content={currentUrl} />
    <meta property="og:type" content={ogType} />
    <meta property="og:site_name" content="VivaSpeak" />
    <meta property="og:locale" content={lang === "es" ? "es_ES" : "en_US"} />
    {lang === "es" && alternateEn && <meta property="og:locale:alternate" content="en_US" />}
    {lang === "en" && alternateEs && <meta property="og:locale:alternate" content="es_ES" />}
    
    <!-- ✅ Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={fullOgImage} />
    
    <!-- ✅ Hreflang -->
   {alternateEs && <link rel="alternate" href={alternateEs} hreflang="es" />}
   {alternateEn && <link rel="alternate" href={alternateEn} hreflang="en" />}
   {alternateEs && <link rel="alternate" href={alternateEs} hreflang="x-default" />}
    <!-- ✅ Favicon -->
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    
    <!-- ✅ Preconnect para recursos externos -->
    <link rel="preconnect" href="https://cdnjs.cloudflare.com" crossorigin />
    <link rel="preconnect" href="https://cdnjs.cloudflare.com" />
    <link rel="dns-prefetch" href="https://www.googletagmanager.com" />
    <link rel="dns-prefetch" href="https://assets.calendly.com" />
    
    <!-- ✅ FontAwesome preload -->
    <link
      rel="preload"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      as="style"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      crossorigin="anonymous"
    />
    
    <!-- Schema.org markup -->
    <SchemaMarkup faq={schemaFaq} breadcrumb={breadcrumb} article={schemaArticle} />
    <!-- Calendly -->
    <script 
      is:inline 
      async 
      src="https://assets.calendly.com/assets/external/widget.js"
    ></script>
  </head>
  <body>
    <slot />

    <!-- Banner de cookies mostrado en todas las páginas -->
    <CookieBanner lang={lang} />
   <script>
  // @ts-ignore – Google Analytics no tiene typings en Astro
  const loadGA = () => {
    const gaScript = document.createElement("script");
    gaScript.src = "https://www.googletagmanager.com/gtag/js?id=G-LVXZ4DBRP0";
    gaScript.async = true;
    document.head.appendChild(gaScript);

    const w = window as Window & {
      dataLayer?: unknown[];
      gtag?: (...args: unknown[]) => void;
    };
    w.dataLayer = w.dataLayer || [];
    const gtag = (...args: unknown[]) => {
      w.dataLayer?.push(args);
    };
    w.gtag = gtag;

    gtag("js", new Date());
    gtag("config", "G-LVXZ4DBRP0");
  };

  const getCookie = (name: string): string | undefined => {
    const value = `; ${document.cookie}`;
    const parts = value.split(`; ${name}=`);
    if (parts.length === 2) {
      const last = parts.pop();
      if (!last) return undefined;
      return last.split(";").shift();
    }
  };
  
  const consent = getCookie("cookie-consent");
  if (consent === "accepted") {
    loadGA();
  }
</script>


    <!-- ✅ JavaScript que falta -->
    <script src="/js/index.js" defer></script>
  </body>
</html>
